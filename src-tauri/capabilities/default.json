{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Overlay plus every tool window. Every window label must match something here — a window that is absent gets no permissions at all, so window.__TAURI__.opener is never injected and tauriBridge.js dies on its first destructure, leaving a blank window. details-* covers the per-fight windows, which are labelled by fight id.",
  "windows": ["main", "settings", "details", "details-*", "history"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
